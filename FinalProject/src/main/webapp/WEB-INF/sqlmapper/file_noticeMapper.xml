<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alj.dream.file_notice.dao.NoticeFileDao">

	<select id="selectCountByNoticeIdx" resultType="int">
		select count(*)
		from file_notice
		where notice_idx=#{idx} and file_deldate is null
	</select>
	
	<update id="deleteNoticeFile">
		update file_notice
		set file_deldate=current_timestamp()
		where notice_idx=#{idx} and file_deldate is null
	</update>
	
	<insert id="insertNoticeFile"
			parameterType="com.alj.dream.file_notice.domain.NoticeFileInfo">
		INSERT INTO file_notice (file_nm, file_size, file_exet, file_originnm, notice_idx)
		VALUES
		(#{file_nm}, #{file_size}, #{file_exet}, #{file_originnm}, #{notice_idx})
	</insert>
	
	<update id="deleteOneFile">
		update file_notice
		set file_deldate=current_timestamp()
		where notice_idx=#{notice_idx} and file_nm=#{file_nm}
	</update>
	
	<select id="selectByNoticeIdx"
			resultType="com.alj.dream.file_notice.domain.NoticeFileInfo">
		select file_nm, file_size, file_exet, file_originnm, notice_idx
		from file_notice
		where notice_idx=#{idx} and file_deldate is null
	</select>

</mapper>